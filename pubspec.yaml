name: datalocal
description: "Flutter dependency to save data via local saved with state, easy to use and fast."
repository: "https://github.com/lamun-my-id/datalocal.git"
version: 1.0.2

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.19.0
  shared_preferences: ^2.2.2
  pointycastle: ^3.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true